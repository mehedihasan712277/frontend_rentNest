import HeroSection from "@/components/core/HeroSection";

const HomePage = () => {
    return (
        <div>
            <HeroSection></HeroSection>
        </div>
    );
};

export default HomePage;
