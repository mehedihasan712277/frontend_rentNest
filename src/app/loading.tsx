export default function Loading() {
    return (
        <main className="min-h-screen w-full flex justify-between items-center bg-background ">
            <p>Loading...</p>
        </main>
    );
}
